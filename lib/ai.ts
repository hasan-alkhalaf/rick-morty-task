export function makeBackstory(name: string, species: string, origin: string) {
  return `${name}, a ${species} from ${origin}, stumbled into multiverse trouble after a tiny mistake. Since then, choices echo across dimensions.`;
}

export function makePersonalitySummary(name: string) {
  return `${name}: High curiosity, average discipline, a bit blunt – inventive but chaotic.`;
}